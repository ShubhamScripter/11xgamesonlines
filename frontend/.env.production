# Frontend served by the same backend → call the API on the same origin.
VITE_API_BASE_URL=/api

# WebSocket auto-derives same-origin (wss) in production, so VITE_WS_URL is
# optional. Uncomment only if the WS host differs from the site host.
# VITE_WS_URL=wss://baajilive.com
